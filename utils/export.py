"""
Export — maneja la exportación de datos a CSV y reportes a PDF.

Responsabilidades:
- Serializar un DataFrame pandas a bytes en formato CSV
- Convertir texto Markdown a un documento PDF descargable

Funciones principales:
    export_to_csv(df)                -> bytes
    export_to_pdf(markdown_text)     -> bytes
"""


def export_to_csv(df) -> bytes:
    pass


def export_to_pdf(markdown_text: str) -> bytes:
    pass
